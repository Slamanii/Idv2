[package]
name = "election_registry"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
# pinocchio = "0.x"    # add when pinning a concrete version on Day 1
sha2.workspace = true
thiserror.workspace = true
