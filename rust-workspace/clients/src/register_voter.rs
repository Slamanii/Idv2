//! register-voter — CLI for submitting a voter identity commitment to Solana.
//! Scaffold only.

fn main() -> anyhow::Result<()> {
    println!("register-voter stub — see TIMELINE.md Day 8");
    Ok(())
}
