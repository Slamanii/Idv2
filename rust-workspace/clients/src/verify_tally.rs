//! verify-tally — permissionless tally verifier.
//! Fetches every BallotAccount, verifies signatures and proofs, prints an
//! audit report. Anyone can run this.
//! Scaffold only.

fn main() -> anyhow::Result<()> {
    println!("verify-tally stub — see TIMELINE.md Day 21");
    Ok(())
}
